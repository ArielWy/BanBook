name: BanBook
version: '${project.version}'
main: me._olios.banbook.BanBook
api-version: 1.20
commands:
  banbook:
    aliases:
      - bb
    usage: /<command>
    description: the main command for the BanBook plugin

  unban:
    usage: /<command>
    description: A check command for unbanning player